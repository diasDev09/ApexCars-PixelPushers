import { DestaquePreco } from './destaque-preco';

describe('DestaquePreco', () => {
  it('should create an instance', () => {
    const directive = new DestaquePreco();
    expect(directive).toBeTruthy();
  });
});
